TestERC4337Account_OnlyEntryPoint
└── given the ERC4337 account with onlyEntryPoint modifier
    ├── when executing a user operation from EntryPoint
    │   └── it should pass validation
    ├── when executing a user operation from a non-EntryPoint address
    │   └── it should fail validation
    └── when validating a user operation with invalid signature
        └── it should fail validation

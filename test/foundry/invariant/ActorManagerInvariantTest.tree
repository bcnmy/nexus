ActorManagerInvariantTest
└── given invariant tests for actor management
    ├── when creating accounts
        └── it should handle account creation correctly
    ├── when managing deposits
        └── it should handle deposits correctly
    ├── when managing withdrawals
        └── it should handle withdrawals correctly
    ├── when checking balances post-revert
        └── it should maintain correct balances
    ├── when testing bounded deposits
        └── it should handle bounded deposits correctly
    ├── when testing decrement failures
        └── it should handle decrement failures correctly
    ├── when testing increment operations
        └── it should handle increment operations correctly
    ├── when installing modules
        └── it should handle module installations correctly
    ├── when installing invalid modules
        └── it should handle invalid module installations correctly
    ├── when uninstalling modules
        └── it should handle module uninstallations correctly
    ├── when uninstalling non-existent modules
        └── it should handle non-existent module uninstallations correctly
    ├── when testing overdraft withdrawals
        └── it should handle overdraft withdrawals correctly
    ├── when testing zero value deposits
        └── it should handle zero value deposits correctly
    ├── when creating multiple accounts with unique indices
        └── it should handle account creation with unique indices correctly
    ├── when checking nonce consistency
        └── it should maintain consistent nonces
    ├── when resetting nonces on account creation
        └── it should reset nonces correctly
    ├── when checking the module address
        └── it should return the correct address
    ├── when checking the module type
        └── it should return the correct type

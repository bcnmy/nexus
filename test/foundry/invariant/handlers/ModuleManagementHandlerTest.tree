ModuleManagementHandlerTest
└── given a Nexus account and signer
    ├── when installing a module
    │   └── it should assert module installation invariant
    ├── when uninstalling a module
    │   └── it should assert module uninstallation invariant
    ├── when checking if a module is installed
    │   └── it should return true if the module is installed
    ├── when installing an invalid module
    │   └── it should revert with an invalid module error
    └── when uninstalling a non-existent module
        └── it should revert with a module not installed error

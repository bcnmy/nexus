BaseAccountInvariantTest
└── given an initialized Nexus account with a MockValidator
    ├── when performing deposit and withdrawal operations
    │   └── it should assert deposit balance consistency invariant
    ├── when attempting unauthorized withdrawals
    │   └── it should assert access control invariant
    ├── when performing a single operation affecting the nonce
    │   └── it should assert nonce consistency invariant
    ├── when performing multiple operations affecting the nonce
    │   └── it should assert multi-operation nonce consistency invariant
    └── when performing multiple operations in a single handleOps call
        └── it should assert multi-operation nonce consistency in a single call invariant
